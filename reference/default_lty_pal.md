# Default linetype palette

The default linetype palette for the graphics. The palette for the
objects with another class than `"in_scatter"` is taken from
`linetype_pal` as seen in the package scales. Hence it is based on a set
supplied by Richard Pearson, University of Manchester.

## Usage

``` r
default_lty_pal(object)
```
